[Enum]
interface FrameEvent {
  Event(u8 channel, f32 amplitude, u16 size);
  Overflow(u8 channel, u16 size);
  Reset(u16 size);
  Frame(u16 size);
};

dictionary NumassEvent {
  u16 offset;
  FrameEvent event;
};

dictionary NumassTrigger {
  u64 time;
  sequence<NumassEvent> events;
};

namespace math {
  u32 add(u32 a, u32 b);
  sequence<u32> vec_from_rust(u32 val);
  sequence<NumassTrigger> process_point(string filepath);
};